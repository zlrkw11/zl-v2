declare module "*.css" {
  // You can leave the body empty, or add a simple export if you expect
  // to import class names (e.g., if you were using CSS Modules).
  // For global CSS, leaving it empty is often enough.
}
